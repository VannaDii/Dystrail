The first before diagnostic failed during compilation, before any trace executed. The five added distance fields pushed one serde_json macro expansion over its existing recursion limit. Original freeze, patch, log and exact-parity immutable reference are preserved here. Correction splits only the same read-only fields into a small nested JSON value; no crate recursion-limit suppression, product/test policy edit, or observation removal.
