The release build reused the prior health045 executable because this snapshot was prepared before that compile. Binary SHA exactly matches health045; release log says0.09seconds and contains no compile. This campaign is INVALID as a pace measurement. The eight pace-specific native tests and seed4242 test did compile from pace-source and passed. Retain this failed verification attempt; rerun release after touching frozen source immediately before build and assert the embedded compiled source path.
