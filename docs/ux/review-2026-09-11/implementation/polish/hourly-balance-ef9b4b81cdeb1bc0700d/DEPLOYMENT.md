# Production delivery — ef9b4b81cdeb1bc0700d

Published commit `5b82fa9d4b08623fa21b70100c196d6af4788606` through [successful Pages run 34758824390](https://github.com/VannaDii/Dystrail/actions/runs/34758824390). Preview 8180 and [production](https://dystrail.com/play/) match. All 201 live payload hashes/MIME checks and six canonical-route checks pass. All 59 protected site files and 25 older hashed assets are preserved. The temporary exact-release-branch permission was removed and the original main-only rule restored.

A disposable prior-production saved installation passes update, offline reload and fully closed-browser offline restart. Its complete stored state is compared, allowing only two defaulted precise-mileage fields and five derived balance settings taken independently from the old/new frozen configuration files. All player stats, time, distance, crew, inventory and UI state are preserved. All 119 cached assets pass byte/hash checks, and 68 images/seven bundled fonts are ready before gameplay. The native OS share sheet and physical phone installation remain unobserved.

Two initial verification failures are retained. The first comparison had omitted the five intentionally refreshed derived rules; it was corrected against both source configurations. The second check clicked an already selected Van tab and correctly collapsed it; the harness now checks selection before clicking. Final verification started again from a fresh copy of the prior-production profile and passes without console errors. No user browser storage was touched.

The unmodified required game client completes two action bursts through character selection with the installed-Chrome adapter, two rendered captures and no game errors. Full validation covers364 native tests, two browser-backed Wasm tests, strict native/Wasm lint and release builds, 2,000 campaigns with unchanged experience guards/strategies, and408 unique desktop/mobile browser cases. The original full run's 394 passes plus 14 corrected-fixture passes are verified as an exact union, with zero skips/flaky results. Failed-run records are retained.

Native Safari is the sole remaining required check. Its two fixture constants now match the accepted balance, and both scripts plus 273 embedded snippets pass static checks. The desktop remains locked as observed at 2026-09-13T13:13:43Z; no native gameplay success is claimed. The earlier unlock request is still pending. No duplicate phone notification was created.
