# Poncho storm protection — 2b0a772d649e1b822dbd

Verified and published to local preview 8180 on September 13, 2026. Production remains `1c145d89fef4fbab75a9`.

This unit extends the verified driving-fatigue source with exactly 23 files: the two approved weather config/regression files, the Storm protection renderer/regression and one added `weather.panel.ponchos` key in each of 20 locales. English, Italian, Spanish and Arabic have native wording; the other 16 preserve the established English fallback convention. Every earlier locale leaf, including pace and narrative text, is unchanged. Only weather.json changes among game-data/config files; camp recovery remains +4. The other 611 frozen inputs are exact. Source tree SHA-256: `a81516fc612803a5207d88f37ba3f28dc42cc23a10380e3f9c23a39a8a271123`.

Actual seed-4 browser travel reproduced the old bug in preview 4172: `rain_resist` was carried but Storm still charged Sanity -1 and had no protection readout. The same equipped and unprotected starts now pass in Classic and Deep: Storm Sanity is 0 with ponchos and -1 without. Both keep Supplies -1, the same miles/time, speed modifier and encounter probability. Daily information-diet effects remain separate from this weather-only receipt.

Thirteen weather tests and all 82 frontend native tests pass (79 library + three integration). Formatting and strict native/Wasm Clippy pass. Four real Storm drives, 48 English/Italian/Spanish/Arabic help checks at 320/393/1440px, and four offline reloads pass. Help correctly labels carried/missing protection, preserves the other costs/modifiers, fits the viewport, closes with Escape, restores focus and spends no stats or time. Offline reload does not repeat weather costs. Browser errors: zero in the corrected run. The required unmodified skill client completed two action bursts and produced two inspected screenshots without error output.

The first Trunk invocation omitted `--public-url /play/`. Its root-relative index URLs caused 404 responses on the isolated test origin; that unpublished artifact and its failed browser/client logs are retained under `first-build-root-url`. The same source was rebuilt with the correct public URL. Before browser navigation, all 12 index asset references, all 119 asset hashes and the embedded worker manifest were checked. Corrected preview publication verifies all 119 served assets (109,966,945 bytes) and the exact worker. No browser save storage was touched.

Reviewed images include carried and missing Storm protection in both palettes, all four translated notes, a short 320px viewport and a wide desktop. The inherited actual-driving fatigue fix and all published content remain included. Experimental camp, health, crossing, activity and arrival changes remain excluded. No production deployment or notification was performed. Locked native Safari remains a known limitation; this unit uses disposable installed-Chrome contexts.
