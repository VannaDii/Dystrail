> Source handoff audit: the earlier completion statement omitted uncommitted source and a stale homepage Pants reference. See [current audit](../../../../release/source-handoff-2026-09-13/README.md).

# Completed delivery — September 13, 2026

Preview port 8180 and [production](https://dystrail.com/play/) match **d3ed78dac458b64a80ee**. The final focus fix is published and verified: 207 live files, six routes, 119 offline assets, saved-install update and offline restart; both-mode native Safari focus and keyboard checks pass. The user's phone installation/offline confirmation closes the physical-device check. [Final evidence and verification boundaries](native-share-focus-d3ed78dac458b64a80ee/README.md), [completed goal](../goal-outstanding-2026-09-12.md).

All original requirements and later feedback are delivered. Evidence below is historical and applies only to its named artifact; older references to outstanding balance, native sharing or physical-device work are superseded by the final record above.

# Completed writer revision and verified preview — September 12

Preview `24834f5498f7778940ee` is published locally. All 101 served assets and the service worker match its frozen artifact; browser saves were untouched. The same build is now verified live at **https://dystrail.com/play/** through [Pages run34740330227](https://github.com/VannaDii/Dystrail/actions/runs/34740330227), commit84dd03513a908f4e2c08b2664acd6258bd9e10d0. All171 published hashes/MIME checks pass; the59 non-game files and approved homepage remain exact. The previous installation updates successfully and a fully closed browser restarts offline with all101 cached assets,67 prepared images and the tested saved crew intact. The temporary branch permission is removed and the main-only rule restored. [Production evidence](writer-24834f5498f7778940ee/DEPLOYMENT.md).

The latest completed workshop supplies all 59 encounters and 169 choices, 28 town comments, 14 crew/ally moments and 11 shared-care strings. Italian, Spanish and Arabic each have all 456 encounter fields and 53 narrative fields. Source recheck 7 confirms the same revision. Authoring instructions and the previously removed one-hour explanation stay outside the game; actual time and resource costs remain in action subtitles. Every gameplay effect, official town fact/source, six newer western encounters and 23 additional town conversations are preserved. The review inventory was regenerated from the frozen release source.

All **406 desktop/phone browser cases pass with exit 0**, including every encounter choice, all revised town conversations, named care and ally outcomes, translated offline play, mobile HUD/help, daily Journal grouping, asset preparation and update recovery. Both registered browser Wasm tests, strict native/Wasm Clippy, formatting, native/Trunk releases and dependency checks pass. The unmodified game client exits 0, and rendered screenshots were inspected. The full native run passes 322 and still fails the same three config/replay checks; gameplay retuning and final coverage remain open. The candidate retains camp recovery +4 and excludes the unverified +6 experiment.

[Validation, logs and screenshots](writer-24834f5498f7778940ee/validation.json) · [Served preview hashes](preview-24834f5498f7778940ee.json) · [Completed writer source mapping](../content/writers-completed-2026-09-12.json). The first new narrative test run retains its fixture failures; the corrected run and full suite preserve the exact time, mileage, effect and outcome assertions. Earlier evidence below remains attributable to its original build and runner exit.

# Writer content and combined mobile preview — September 12

Local preview `fd85d61ff87f8e40619b` is published at port 8180. The 101 assets and service worker match the immutable artifact, and saves were untouched. It includes the verified mobile HUD/help and daily Journal, the ordinary-health cause and stationary-time fixes, and 22 new writer fields across three scenes. Italian, Spanish and Arabic receive 72 translated fields. It retains camp recovery +4; the +6 balance experiment remains separate.

The full desktop/phone browser run passes 208 cases. Its six new writer fixtures initially used an incorrect JSON URL; after correcting that path and checking the response MIME type, all six pass with unchanged copy, outcome and stat assertions. Thus all 214 cases pass across the two runs on this same artifact. Two registered browser Wasm tests, native/Wasm strict lint, formatting, native/Trunk release builds, advisory-exception validation, Cargo audit and Cargo deny pass. The full native run passes 319 and retains three pending configuration/replay baselines. The unmodified game-client exits 0; actual phone and game-client screenshots were inspected.

The user says the writer document is being updated with real responses. Three placeholders remain excluded, and source refreshes still return the captured revision. Production is held for the completed copy and another source revision check. The prepared release preserves the approved live homepage. [Validation and screenshots](writer-fd85d61ff87f8e40619b/validation.json). Earlier evidence below is historical and retains its original failures and exits.

# Mobile impact help and daily Journal preview — September 12

Local preview `ebba9ffd5912cc71a21c` is published at port 8180. The phone HUD uses the full available width. All six policy explanations and five weather explanations use structured cost, protection and timing sections; popups stay inside short phone viewports and retain a reachable close button. Keyboard opening, closing and focus restoration are covered. Journal entries accumulate by day, preserve original actions and correct the final driving-hour date. The unchanged compact desktop height guard passes.

All 86 focused desktop/phone checks pass with exit 0. The remaining regression suite passes 120 and fails six assertions across three paired camp/map scenarios; those failures are being investigated and retained. Native17 passes313 tests with only three pending config/replay snapshot expectations failing. Native/Trunk releases and strict native/Wasm lint pass within the recorded scopes. The unmodified game client on the published preview exits0; its rendered screenshot and phone/Journal screenshots were inspected. All101 assets and the service worker match, and saves were untouched.

[Served bytes](preview-ebba9ffd5912cc71a21c.json), [unit validation and screenshots](mobile-journal-ebba9ffd5912cc71a21c/validation.json). This is a local playtesting build with five active hours and Mixed+1. It does not include later health-cause or camp-recovery experiments. Campaign07 still fails retained experience bands; production game remains08a and the approved homepage is already live.

# Mobile status-bar preview — September 12

Local preview `f86b762b4caad222fd20` is published at port 8180. It adds only the verified mobile HUD CSS to the earlier135 scene/engine artifact. Resource groups use clean four-plus-three rows; settings, destination, Cash/Vehicle and temporary effects align across the available width. English/Arabic checks at320/393/600/1440 show no overflow or Help & tips layout shift. All101served assets and the worker match; user save storage was untouched. Evidence: [served bytes](preview-f86b762b4caad222fd20.json), `/tmp/dystrail-hud-surface/patched.json`.

The combined structured-popup and daily-Journal source is still under browser verification. Its actual rollover regression exposed the final drive stamped on the next day's Journal; the source fix preserves that drive in its completed day without changing miles or time. The game currently in production remains08a; the approved homepage CTA and versioned stylesheet are verified live via Pages run34732895218.

# Current local scene preview — September 12

Local preview `135051c37b00eb6eeff0` is published at port 8180. The stopped crew forms a compact discussion group with depth and readable faces to the right of the parked van; foreground fence silhouettes cover both actors correctly. Actual driving-duration subtitles and the verified eight-hour engine are retained while the five-hour balance candidate remains separate. All 101 assets and the service worker match the frozen artifact; saves are untouched.

Four scene checks and ten hourly-travel checks pass on Chrome desktop/phone (14 total, exit 0). The unmodified web-game client exits 0 and screenshots were inspected. Native Safari passes the shop, crew/offline, paid-shift return and Van checks before timing out at Journal; that is a partial run, not full completion. Current evidence: [served bytes](preview-135051c37b00eb6eeff0.json), `/tmp/dystrail-discussion-preview-browser.log`, `/tmp/dystrail-discussion-preview-safari/validation.json`, and `/tmp/dystrail-discussion-preview-game-client/`.

Production remains the explicitly requested `08a07362f3e5c5445818`, verified at https://dystrail.com/play/. The approved homepage CTA is a separate HTML/CSS-only publication. One-entry-per-day Journal work and retained campaign acceptance failures are open; see the [current goal](../goal-outstanding-2026-09-12.md). All evidence below is historical and applies only to its named build.

# Pulled-over scene clarification — September 12

Revision `08a07362f3e5c5445818` is now served at port 8180 and 56854. The van is parked on the lower shoulder, with all present crew grouped to its right. Verified with four Chrome desktop/phone cases, 16 native Safari groups, offline recovery and the unmodified game client; all exited 0 and screenshots were inspected. All 101 served assets and the service worker match the published manifest. Evidence: `/tmp/dystrail-pullover-*` and `preview-08a07362f3e5c5445818.json`.

Fresh-browser pacing evidence: [hourly measurements](../campaign/live-hourly-pacing-08a07362f3e5c5445818.json). Steady60/Heated70/Blitz80physical miles per clear hour; eight Steady hours480miles; two-hour forage leaves360miles. The hourly campaign acceptance work below remains open. Existing running pages retain their loaded engine until the next reload and loading-gate update.

# Previous HUD and return-navigation unit — September 12

Revision `ae87262a0b1ff468dbf4` was served at http://127.0.0.1:8180/play/. The consolidated top HUD, removal of the bottom bar/turn indicator, stopped-crew sprites, and paid-shift Back to town return are published. All 101 files and the service worker are verified; save storage was not touched.

Validation for this unit: 152 engine unit tests, 4 hazard-distribution tests, 55 native web tests, strict native/Wasm Clippy, 2 registered Wasm tests, Trunk release, 10 focused Chrome desktop/phone cases followed by 4 final-art cases, 16 native Safari groups, offline recovery, Arabic layout and the unmodified web-game client. All named checks exited 0. Desktop, mobile, native Safari and game-client screenshots were inspected. Evidence: `/tmp/dystrail-stopped-crew-*` and `preview-ae87262a0b1ff468dbf4.json`.

**Remaining acceptance:** the full native run still has four failing targets containing older day/mileage unit assumptions, config/deterministic snapshots, and campaign expectations. Campaign resource, stopping, encounter-variety and victory thresholds remain unchanged; tuning is still in progress. This preview includes the new hourly mechanics for active playtesting and is not a completed balance release.

---

# Historical full-regression preview: 7050e40f35ec177a2368

The title, Journal divider, rounded-tab and phone-overflow fixes are published at port 8180. All 186 Chrome desktop/phone regression cases completed successfully (exit 0, 5.7 minutes). Focused title/layout checks and all 15 native Safari groups also completed with exit 0. The current hourly travel and balance work is an unpublished candidate.

[Latest validation record](entry-polish-validation.json). Historical evidence below remains attributable to its original builds.

# Current preview and verification

September 12, 2026. [Play the preview](http://127.0.0.1:8180/play/). Build **56213723b1c62ed89b93**. The older Safari preview on port 56854 and isolated preview on 8083 serve identical bytes. All 100 assets, 104,083,461 bytes, and the service worker were verified. Publishing did not modify browser storage.

## Delivered in the latest units

- The Van shows its crew before parts and equipment, with an illustrated Trip overview for supplies, cash, vehicle condition and mileage. Cash and distance received new pixel artwork; all images are included in the loading gate.
- Every entry from the current player turn appears under The Trail, newest first, and survives navigation/reload. Starting travel begins a fresh Trail report while retaining the complete Journal.
- Trail and Journal share 110px stat cards, arranged beside the narrative when space permits. Five cards fit on one desktop row. Phone cards wrap without clipping or shrinking below their intended size. Journal date/time, pace and diet sit below the route with icons; settings are saved with the original action.
- Scene captions and titles use a soft black text outline over artwork.
- Outcome text and changes use a compact responsive layout. A single Back to the road button stays reachable as the viewport scrolls. Chrome tests cover 800, 600 and 420px viewport heights, including a long saved outcome; native Safari verifies visibility and clickability across scroll positions.
- Manual Route views show Close map, preserve the previous screen and selected tab, and never start travel. This survives reload. Automatic maps retain the three-second, one-click travel behavior; Fast mode never auto-opens them.
- Clicking the active Trail, Conditions, Van or Journal tab hides its content and clears its selection. Clicking again reopens it. Keyboard navigation, focus, and saved selection remain available.

Each completed unit was published incrementally. [Current served-byte record](preview-56213723b1c62ed89b93.json), [all preview origins](served-assets.json). Earlier revision records remain in this directory.

## Verification

- 267 native tests, both registered Wasm/Chrome tests, strict Clippy on native and Wasm, native/Trunk release builds, rustfmt and diff checks pass. Engine coverage is 78.81% (3,511/4,455 lines), above the required 77%.
- All six new desktop/phone interaction tests pass their assertions. The isolated phone rerun exits 0. Desktop shutdown still intermittently hangs after passing its three cases; those runs retain exit 1.
- All 15 desktop regression cases pass with exit 0. All 15 phone cases pass their assertions, but that runner times out during shutdown. Automatic map timing, held maps, manual closing, queued crew decisions, outcomes and current-turn history are covered.
- Native Safari passes all 14 inventory/status/navigation/locale groups and all six offline/update groups with exit 0. With its origin stopped, every one of the 100 cached assets matches its hash and the complete saved game relaunches. Completed and interrupted updates preserve the save.
- The unmodified game-client smoke exits 0; its screenshots and targeted desktop/phone/Safari screenshots were visually inspected.
- All 20 locale files contain the 1,099 required keys, including translated Close map and Trip overview labels. A native Safari runtime smoke renders Journal and manual maps in every locale; Arabic and Japanese screenshots were inspected. New rich narrative content still falls back to English in 16 locales; key completeness does not imply a human translation review.
- Dependency checks exit 0 with the existing dated maintenance exceptions and warning output retained. No dependency policy was relaxed.

[Exact results](validation.json), [commands and logs](commands.md), [latest evidence](units-2026-09-12/). Historical failures are preserved, including the mobile clipping found during visual review and the Chrome shutdown timeouts. Neither is relabeled as a clean pass.

## Remaining work and limitations

The [campaign decision](../campaign/balance-decision.md) is still open. The latest 8,000-run measurement includes the final repair/reward rules and fails the unchanged acceptance bands; recent presentation changes do not alter that gameplay. No gates were weakened.

Chrome's intermittent runner shutdown remains unresolved. Native Safari and the actual interaction assertions pass; clean completion is reported only for runs that exit 0. Physical-device home-screen installation has not been performed.

See the [current goal and remaining work](../goal-outstanding-2026-09-12.md) and [content/choice inventory](../content/index.html).


## September 13 visual pass

Preview and production a9ff83cd924a07fdf4ec are verified, including all 194 live payload hashes and the saved-game update/offline restart. [Final visual evidence](visual-a9ff83cd924a07fdf4ec/README.md) records the Deep palette, compact phone HUD, local fonts, encounter artwork, results/share refinement, the original three mobile failures and their final 80-case regression pass. Engine and content remain identical to the preceding writer release. Gameplay retuning remains open.
