# Website and playable game release — September 11, 2026

This iteration combines the redesigned Dystopian Trail homepage with the reviewed local game, sourced political encounters, updated journey and crew flow, town interactions, and offline support.

The deployment source is `release/website-game-2026-09-11`, based on `e38e5ab72c1d81eef99cd6c90febfb0913783bef`. The default branch has a separate 112-commit engine history and is preserved. The release snapshot is isolated from continued development in the original checkout.

## Validation

- Fresh Trunk release and mdBook builds passed.
- `cargo fmt --all -- --check` passed.
- `cargo test --workspace --all-features`: 234 passed, zero failures.
- Nine focused Chromium browser checks passed, including full offline asset availability, saved play after offline relaunch, town scenes, complete update activation, interrupted-update fallback, and retained saves.
- The offline build fingerprints 79 files totaling 75667959 bytes. Offline play becomes available after the initial online load and successful asset preparation.
- Homepage review measured Lighthouse 100 in all desktop categories and 99 performance / 100 accessibility / 100 best practices / 100 SEO on mobile. Desktop/mobile layouts, keyboard access, reduced motion, and 200% text enlargement were checked.

This is an iterative release, approved for publication with continued game development. Existing dependency-audit and campaign-balance findings remain recorded in the development workspace; this release does not claim a clean security audit or final game balance. No checks were weakened to hide those findings.

The existing GitHub Pages workflow builds `/`, `/play/`, and `/docs/` together. Cargo.lock is included to retain the tested dependency resolution. Local player saves were preserved by using a separate test origin and temporary browser profiles.
