Road family C01 now has six authored pixel-art frames: park cleanup, repair co-op and print-shop yard, each with an untouched offer and a worked site. The three variants retain the same two existing engine choices and their ordered effects. The current workshop prose remains authoritative.

Helping selects the worked frame only after the engine commits that choice. Photographing preserves the untouched site while the actual receipt and outcome prose show the acquired evidence. `SceneStage::EncounterOutcome` stores the selected unit and committed choice in the recovery checkpoint. No scene selection draws simulation randomness, and no new encounter effects are introduced. Legacy checkpoints containing only `Encounter(unit)` remain readable; without a recorded choice the renderer conservatively keeps the untouched site and preserves the saved outcome text and effects.

The ranger, mechanic, salvage volunteer, printer and lunch volunteer belong to their authored settings. They are feminine or neutral supporting residents, distinct from the current crew. The runtime crew portrait follows actual active membership; departed characters cannot appear through a preferred subject ID. Removed the old independent local portrait for these settings, which otherwise showed a second differently drawn resident. The larger supporting-cast audit remains required elsewhere.

The machine display and bin cards use native SVG lettering on their actual blank props. The machine message is split into two localized lines in English, Spanish, Italian and Arabic, with English fallback elsewhere. The typeface names retain their actual names; sans-serif and serif rendering make the distinction visible. Matching text outside the decorative art is available to screen readers. Full frames sit between the conditions HUD and separate title/crew footer, preserving props on phone screens. Existing real clock and weather rendering apply; the repair co-op is indoors.

The raster was generated with the built-in image tool and copied unchanged to `dystrail-web/static/img/scenes-v2/road-c01-20260914.png`. Original: `exec-f0870b73-32b8-4f43-9478-b716aef62922.png`. Exact prompt: `review/art-satire/prompts/road-c01-20260914.txt`. Source bounds and hashes are recorded in `review/art-satire/asset-catalog.json`. Earlier background-only renders remain historical evidence.

Translation corrections are narrowly recorded in `review/art-satire/road-c01-copy-changes.json`: Spanish giver/recipient wording, Arabic ranger/mechanic references, Italian stray apostrophe escapes and new prop-label line breaks. The English narrative and accepted hearing translation subtrees are unchanged.

Validation is in progress on `e6f8388335c441509db2`: 99 web library tests and strict native lint passed; all 153 offline assets have verified lengths and hashes. Seven hearing engine/tester files, twenty hearing locale subtrees and sixty source snapshot files are preserved. Desktop/phone choice, localization, old/new checkpoint, offline and combined hearing checks must finish before the three units are marked reviewed. Cross-release content revision pinning, remaining scene families and final release acceptance are still outstanding. Nothing committed or deployed.
