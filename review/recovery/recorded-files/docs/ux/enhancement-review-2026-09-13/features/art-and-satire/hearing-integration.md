# Combined hearing and visual-world delivery

On September 14 the user asked this task to incorporate all work from **Round 2 Features - Hearing Model** and deliver the combined outcome. The other task is idle. Its 60-file snapshot still matches its source checkout; the source checkout has not been overwritten.

The combined feature retains the accepted hearing mechanics: existing stat-derived starting odds, one to three rounds, independent 50% continuation checks, uniform 50–150% influence, two sanity per actual round, exhaustion precedence, averaging only completed rounds, automatic success only after questioning closes at adjusted odds of at least 100%, and the original final-vote draw otherwise. The existing Deep/Aggressive guarantee and accepted campaign threshold remain intact. All seven captured engine/tester files remain byte-identical.

The saved hearing report and presentation cursor, player-held arrival, preparation/rest flow, round receipts, committee checks, final calculation, distinct verdicts, Fast/Skip/reduced-motion behavior, and recovery paths are retained. The core hearing page, timing and summary components remain identical to the other task. The complete `hearing` translation subtree is unchanged in all twenty locales.

Presentation adapters replace the old cast with the new consistent portraits, expressions and transparent standing poses, including scorecard and downloaded share-image appearances. They retain names, active-crew filtering and actual report outcomes. Real alpha replaces the older chroma-key workaround, preserving its purpose without depending on a magenta matte. Outcome copy consumes the committed report, including short hearings, exhaustion and secured victory.

The earlier implementation package, later feedback package, transparency-fix evidence, probability audit and portable pre-hearing save have been imported intact under `../../implementation/hearing/`. Their original build revisions and validation counts are historical evidence. `review/art-satire/hearing-documents-import.json` records all 113 imported documents and artifacts. Combined-build evidence is recorded separately in `review/art-satire/policy-bc-review.json`; do not treat a historical hearing build as the current combined release.

The completed hearing feature is part of this delivery. The larger visual-world feature still requires the remaining scene/content coverage, supporting-cast and derived-art audits, and final release/update verification. No deployment is authorized by this integration handoff.
